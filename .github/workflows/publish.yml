name: Publish Package to npmjs
on:
  release:
    types: [created]
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
    inputs:
      node-version:
        description: 'Node.js version'
        required: false
        default: '18.x'

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]

    permissions:
      contents: read
      id-token: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        registry-url: 'https://registry.npmjs.org'

    - name: Install dependencies
      run: CI=true yarn install

    - name: Set npm scope
      # Should be set in the repository's variables, current value now is: numerator-io
      run: npm set scope=@${{ github.repository_owner }}

    - name: Publish package
      run: npm publish --provenance --access public
        env:
          # FIXME: ${{ secrets.NPM_TOKEN }} should replace and be set in the repository's secrets
          NODE_AUTH_TOKEN: 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
